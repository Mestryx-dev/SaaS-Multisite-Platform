/** Storybook globals for dual-theme toolbar (`preview.tsx` globalTypes.theme). */
export const platformGlobals = { theme: "platform" as const };
export const storefrontGlobals = { theme: "storefront" as const };
